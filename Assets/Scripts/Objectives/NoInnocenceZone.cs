using UnityEngine;

public class NoInnocenceZone:MonoBehaviour
{
    /*private void OnTriggerStay(Collider other)
    {
        Debug.Log("called " + other.name);
        var innocence = other.GetComponent<Innocence>();
        if(innocence!=null)
        {
            innocence.insideHouse = true;
            if (innocence.Using)
                innocence.OnTryUnuse();
        }
    }

    private void OnTriggerExit(Collider other)
    {

        Debug.Log("called " + other.name);
        var innocence = other.GetComponent<Innocence>();
        if (innocence != null)
        {
            innocence.insideHouse = false;
        }
    }*/

}
